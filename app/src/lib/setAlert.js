{
  /*import React from 'react'
import { subtract } from 'ramda'
import { GET_CURRENT_REMINDER } from '../constants'
import { connect } from 'isomorphic-fetch'

const AlertAt = props => {
  const setHours = subtract(
    props.reminder.dueAtHours,
    props.reminder.remindHrsBefore
  )
  return <div>{setHours}</div>
}

const mapStateToProps = state => ({
  reminder: state.currentReminder
})

const connector = connect(mapStateToProps)
export default connector(AlertAt)*/
}
